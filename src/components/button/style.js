import { COLOR } from "react-native-material-ui";

export default { 
    container: {
        width: 50,
        height: 50,
        justifyContent: 'center',
        alignItems: 'center'
    },
    text: {
        fontSize: 30,
        color: COLOR.black
    }
};